/**
 * 인기 순위 — 매월 말일, 계정 합계 팔로워로 1~100위를 매긴다.
 *
 * 왜 넣었나: 트윗은 이 게임의 기본 행동인데, 팔로워 숫자가 늘어나는 것 말고는
 * "잘하고 있다"는 신호가 없었다. 100만은 너무 멀어서 중간 목표가 되지 못한다.
 * 매달 순위가 나오면 한 달치 트윗이 눈에 보이는 자리로 바뀐다.
 *
 * 규칙(문턱 계산·집계·중복 방지)은 `systems/popularity.ts`가 소유한다.
 *
 * ⚠️ **1위 = 95만**이다(목표 100만의 바로 아래). 1위를 찍고도 게임이 안 끝나는 구간을
 *    일부러 남긴 것이다 — 정상에 서 본 뒤 마지막 5만을 채우는 게 이 게임의 마무리다.
 */

/** 1위가 되는 팔로워 수(계정 합계). */
export const RANK1_FOLLOWERS = 950_000;
/** 순위표의 꼴찌(100위) 문턱. 이 밑은 순위권 밖이다. */
export const RANK_LAST_FOLLOWERS = 100;
/** 순위표 크기. */
export const RANK_SIZE = 100;

/**
 * 순위 구간별 한 줄 — 발표 화면이 순위 아래에 붙인다.
 * 실제 SNS 팔로워 분포처럼 위로 갈수록 급격히 좁아지므로, 문구도 그 체감에 맞춘다.
 */
export interface RankTier {
  /** 이 순위 **이하**(더 좋은 순위)면 이 문구를 쓴다 */
  upTo: number;
  label: string;
  line: string;
}

export const RANK_TIERS: readonly RankTier[] = [
  {
    upTo: 1,
    label: "1위",
    line: "이 나라에서 이 앱을 쓰는 사람 중 당신보다 팔로워가 많은 사람은 없다. 캡처가 온 타임라인에 돌아다닌다.",
  },
  {
    upTo: 3,
    label: "최상위권",
    line: "이름 석 자만 검색해도 연관 검색어가 줄줄이 붙는다. 1위 자리가 손에 닿는 곳에 있다.",
  },
  {
    upTo: 10,
    label: "톱10",
    line: "월간 순위 캡처에 당신 계정이 박혔다. 모르는 사람이 아는 사람처럼 말을 건다.",
  },
  {
    upTo: 30,
    label: "상위권",
    line: "타임라인에서 이름이 자주 보이는 축에 들었다. 협찬 문의 메일이 늘기 시작한다.",
  },
  {
    upTo: 60,
    label: "중위권",
    line: "순위표 가운데쯤이다. 위도 아래도 빽빽해서, 한 달만 쉬어도 자리가 바뀐다.",
  },
  {
    upTo: RANK_SIZE,
    label: "순위권",
    line: "100등 안에 이름을 올렸다. 표에 실린 것과 안 실린 것의 차이는 생각보다 크다.",
  },
];

/** 순위권 밖일 때의 문구. */
export const RANK_UNRANKED_LINE =
  "이번 달 순위표에는 이름이 없다. 100등 안에 들려면 팔로워가 더 필요하다.";

/** 지난달보다 오른/내린/그대로일 때 덧붙이는 한 줄. */
export const RANK_DELTA_LINES = {
  up: (n: number): string => `지난달보다 ${n}계단 올랐다.`,
  down: (n: number): string => `지난달보다 ${n}계단 내려갔다.`,
  same: "지난달과 같은 자리다.",
  first: "첫 순위 발표다.",
} as const;
